{
  "name": "example-cloudflare-hono-vite",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "pnpm build && vite preview",
    "deploy": "pnpm build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "catalog:",
    "@hanzo/insights-node": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "@hanzo/rollup-plugin": "catalog:",
    "vite": "catalog:",
    "wrangler": "catalog:"
  }
}
